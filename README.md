# color-corrector-to-aces-standards
this script can be run straight from the console run on any os with no errors. it will match Aces prebuilt colors in davanci and has room to be altered per each need.
